<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>CRUD Operation|Node.js|MySql</title>

  <!-- bootstrap link  -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>

  <!-- [ <% %> ] This is a template engine tag. It is used to embed the JavaScript code in the HTML file. -->


  <div class="container">
    <h1 class="text-center">READ Operation</h1>

    <div class="row mx-2">

     <% if(rows.length === 0){ %>
        <h1 class="text-center mt-3 text-danger">No Data Found</h1>

    <%  }else{ %>

        <% rows.map((eachData)=>{ %>
          <!-- <div class="col-md-4 mx-2"> -->
  
            <div class="card mt-3 mx-2" style="width: 18rem;">
              <div class="card-body">
                <h5 class="card-title"> <%- eachData.name %> </h5>
                <h6 class="card-subtitle mb-2 text-body-secondary"><%- eachData.email %></h6>
                <p class="card-text"><%- eachData.phone %></p>
                <p class="card-text"><%- eachData.occupation %></p>
                <a href="#" class="card-link">Update</a>
                <a href="/delete-data?id=<%-eachData.id%>" class="card-link">Delete</a>
              </div>
            </div>
  
          <!-- </div> -->
  
          <% }) %>

     <% } %>

      

    </div>

  </div>

</body>

</html>